<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="platinum.whatstheplanserver">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LandActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.EventsActivity" />
        <activity android:name=".activities.authentications.SignInActivity" />
        <activity android:name=".activities.authentications.SignUpActivity" />
        <activity android:name=".activities.authentications.NameSubmitActivity" />
        <activity android:name=".activities.authentications.PasswordResetActivity" />
        <activity android:name=".activities.NewEventSecondActivity" />
        <activity android:name=".activities.NewEventFirstActivity" />
        <activity android:name=".activities.NewEventThirdActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.VenuesActivity" />
        <activity android:name=".activities.NewVenueFirstActivity" />
        <activity android:name=".activities.NewVenueSecondActivity" />
        <activity android:name=".activities.NewVenueThirdActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.VenueOnMapActivity"
            android:label="@string/title_activity_venue_on_map" />
        <activity android:name=".activities.NewEventFourthActivity" />
        <activity android:name=".activities.BookingsActivity"></activity>
    </application>

</manifest>